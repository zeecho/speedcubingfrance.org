export const selfInfoUrl = () => {
  return "<%= Rails.application.routes.url_helpers.self_info_path %>";
};
/*
export const roundResourceUrl = id => {
  return `<%# URI.decode(Rails.application.routes.url_helpers.round_path("${id}", format: :json)) %>`;
};

export const personResourceUrl = id => {
  return `<%# URI.decode(Rails.application.routes.url_helpers.person_path("${id}", format: :json)) %>`;
};
*/
