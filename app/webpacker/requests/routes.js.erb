export const selfInfoUrl = () => {
  return "<%= Rails.application.routes.url_helpers.self_info_path %>";
};

export const loadResultUrl = id => {
  const event_id = id.eventId;
  const competition_id = id.competitionId;
  return `<%= CGI.unescape(Rails.application.routes.url_helpers.show_result_path("${event_id}", "${competition_id}", format: :json)) %>`;
};

export const submitResultUrl = id => {
  const event_id = id.eventId;
  const competition_id = id.competitionId;
  return `<%= CGI.unescape(Rails.application.routes.url_helpers.submit_result_path("${event_id}", "${competition_id}", format: :json)) %>`;
};

export const resultsListUrl = id => {
  const event_id = id.eventId;
  const competition_id = id.competitionId;
  return `<%= CGI.unescape(Rails.application.routes.url_helpers.results_for_comp_path("${event_id}", "${competition_id}", format: :json)) %>`;
};

export const destroyResultUrl = id => {
  return `<%= CGI.unescape(Rails.application.routes.url_helpers.destroy_result_path("${id}", format: :json)) %>`;
}
