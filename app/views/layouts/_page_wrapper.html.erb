<% menu ||= {} %>
<% active ||= "" %>
<% card_classes ||= "" %>
<%= render "layouts/subnav", elements: menu, active: active %>

<h2 class="page-title text-center"><%= title %></h2>

<div class="card <%= card_classes %>">
  <div class="card-body">
    <%= yield %>
  </div>
</div>
