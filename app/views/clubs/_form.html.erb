<%= simple_form_for(@club) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, label: 'Nom' %>
    <%= f.input :website, label: 'Site internet' %>
    <%= f.input :email %>
    <%= f.input :description %>
    <%= f.input :logo %>
    <%= f.association :department, collection: Department.c_all_by_id.values.sort_by(&:code), label: ("Département"), :label_method => lambda { |dep| "#{dep.code} #{dep.name} (#{dep.region.name})" } %>
    <%= f.input :city, label: 'Ville' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
